# Build to zero ts typeorm example project
- npx typeorm init --database sqlite3

# Install typeorm encrypted
- npm install --save typeorm-encrypted --legacy-peer-deps

# Start the API
- npm run watch